# configs/data/default.yaml

_target_: src.data.datamodule.UMAFoldDataModule
target_dir: ${paths.data_dir}/raw/rcsb_processed_targets
msa_dir: ${paths.data_dir}/raw/rcsb_processed_msa
symmetries_path: ${paths.data_dir}/raw/symmetry.pkl

# Aggressive cropping for 24GB VRAM
max_tokens: 256
max_atoms: 2304

batch_size: 1
num_workers: 4