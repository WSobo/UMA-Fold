# Core deep learning
torch>=2.2.0
torchvision>=0.17.0

# PyTorch Lightning – training loop abstraction
pytorch-lightning>=2.2.0

# Hydra – hierarchical configuration management
hydra-core>=1.3.2
omegaconf>=2.3.0

# Experiment tracking
wandb>=0.16.0

# Tensor manipulation helpers
einops>=0.7.0

# Equivariant neural network primitives — intended for future use as an
# optional equivariant feature encoder for atomic-level inputs.
# Install separately once a matching wheel is available for your CUDA version:
#   pip install cuequivariance
# cuequivariance>=0.1.0

# Structural biology / data I/O
biopython>=1.83
numpy>=1.26.0

# Visualisation helpers (PyMOL must be installed separately via conda or the pymol.org wheel)
# pymol-open-source>=2.5.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Code quality
ruff>=0.4.0
