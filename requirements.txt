# ==============================================================================
# UMA-Fold Dependencies
# ==============================================================================
# The canonical dependency source is pyproject.toml, managed by uv.
# Install everything with:
#
#   uv sync --extra dev
#
# That's it — no conda, no manual boltz clone, no separate pip steps.
# uv handles PyTorch (CUDA 12.4 wheels) and Boltz (PyPI) automatically.
#
# This file is kept for reference / pip fallback only.
# ==============================================================================

# PyTorch Lightning – training loop abstraction
pytorch-lightning>=2.2.0

# Hydra – hierarchical configuration management
hydra-core>=1.3.2
omegaconf>=2.3.0

# Experiment tracking
wandb>=0.16.0

# Tensor manipulation helpers
einops>=0.7.0

# Structural biology / data I/O
biopython>=1.83
numpy>=1.26.0

# Testing & Code quality
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0
